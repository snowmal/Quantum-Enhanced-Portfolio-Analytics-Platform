# Quantum-Enhanced Portfolio Risk System
# =======================================
# Install all core dependencies:
#     pip install -r requirements.txt
#
# Then install optional dependencies as needed (see sections below).

# ── Data pipeline ─────────────────────────────────────────────────────────────
yfinance>=0.2.40          # live + historical market data (data_pipeline.py, live_optimizer.py)
fredapi>=0.5.1            # FRED macroeconomic variables (VIX, yield curve, FFR)
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0             # SLSQP optimizer, norm distribution (VaR/ES)
scikit-learn>=1.4.0       # StandardScaler for FHE weight encoding
joblib>=1.3.0             # parallel Monte Carlo paths (projection_engine.py)

# ── Dashboard ─────────────────────────────────────────────────────────────────
dash>=2.16.0
plotly>=5.20.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.31.0
pytz>=2024.1              # market hours detection (live_optimizer.py)
tabulate>=0.9.0           # comparison tables (run_fhe_comparison.py)

# ── FHE / Encrypted pipeline (required for FHE COMPARISON tab) ───────────────
#
#   tenseal is the CKKS homomorphic encryption library used by:
#     - encrypted_classical.py   (full Alice → Carol → Alice pipeline)
#     - run_fhe_comparison.py    (quick plaintext vs. CKKS accuracy check)
#
#   Without tenseal the dashboard runs fully — all tabs except FHE COMPARISON
#   work without it. The FHE tab shows a clear install prompt when missing.
#
#   Install:
#     pip install tenseal
#
#   Note: tenseal requires a C++ build toolchain. On macOS, ensure
#   Xcode Command Line Tools are installed: xcode-select --install
#   On Linux: sudo apt install build-essential
#
tenseal>=0.3.14

# ── Quantum layer (required for quantum pipeline modes — Phase 2) ─────────────
#
#   PennyLane is used for VQC training (vqc_portfolio.py — pending).
#   Not needed until Phase 2 quantum modules are built.
#
#   Install:
#     pip install pennylane pennylane-lightning
#
#   For GPU-accelerated VQC training on NVIDIA hardware:
#     pip install pennylane-lightning[gpu]    # requires CUDA
#
# pennylane>=0.36.0
# pennylane-lightning>=0.36.0

# ── HPC / cluster (optional) ──────────────────────────────────────────────────
#
#   mpi4py enables true distributed-memory parallelism for Monte Carlo
#   path simulation on SLURM/PBS HPC clusters (mpirun mode).
#   Requires an MPI runtime (OpenMPI or MPICH) installed on the system.
#
#   Install:
#     sudo apt install libopenmpi-dev   # or: brew install open-mpi
#     pip install mpi4py
#
# mpi4py>=3.1.0